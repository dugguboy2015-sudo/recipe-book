# Recipe Book — Build Progress Log

## Phase 0 — Preflight, backup, emergency lockdown — DONE
- Date: 2026-09-10
- Branch / PR: phase-0-lockdown / [#1](https://github.com/dugguboy2015-sudo/recipe-book/pull/1) (merged 83ab4b3)
- Migrations applied: 000_lockdown.sql, 000a_lockdown_maintain.sql
- Backup: backups/2026-09-10T22-31-51-087Z (32 rows; recipes.json, policies.json, grants.json, columns.json, rls.json)
- Acceptance:
  - [x] Preflight: all required checks PASS (Node 22.18, gh as dugguboy2015-sudo with push, Supabase secret key + SQL API, Cloudflare token + Pages project, Workers AI, Turnstile secret + site key)
  - [x] Backup folder exists; recipes.json holds 32 rows, verified against `select count(*)`
  - [x] All 0.9 probes return the stated statuses: PATCH/POST/DELETE with publishable key → 401, code 42501; GET → 200 with 30 rows; GET is_deleted=true → 200 `[]`
  - [x] `schema_migrations` contains 000_lockdown.sql (and 000a_lockdown_maintain.sql, see deviation below)
  - [x] RLS audit (A.0, corrected per deviation below) returns zero rows
- Preview smoke: PASS https://8739ea62.recipe-book-9eo.pages.dev (no `npm run smoke` yet — Phase 1 adds it; verified manually: pages 200, publishable-key probes match)   Production smoke: PASS https://recipe-book-9eo.pages.dev (all three pages 200; GET returns 30 rows; POST without the write API → 401/42501)
- Deviations from spec:
  1. **Appendix A.0's third audit query is unreliable and was replaced.** The Supabase Management API's `/database/query` endpoint always executes as `supabase_read_only_user` regardless of the `read_only` flag (confirmed: `select current_user` returns `supabase_read_only_user` even with `read_only: false`). `information_schema.role_table_grants` only surfaces grants where the *current* role is grantor/grantee/PUBLIC, so querying it from this role can never see grants held by `anon`/`authenticated` — it always returns zero rows, which would be a false pass even with no lockdown applied at all. Verified this by running the original query against the *pre-lockdown* database: it returned `[]` despite `anon` holding every privilege. Replaced it with a query over `pg_class.relacl` via `aclexplode()`, which is visible to any role with `SELECT` on `pg_class`:
     ```sql
     select c.relname, a.privilege_type, a.grantee::regrole::text as grantee
     from pg_class c, aclexplode(c.relacl) a
     where c.relnamespace = 'public'::regnamespace and c.relkind = 'r'
       and a.grantee::regrole::text in ('anon','authenticated') and a.privilege_type <> 'SELECT';
     ```
     This becomes the standing A.0 audit for the rest of the project. The first two A.0 queries (RLS enabled, security_invoker views) behaved as documented and are unchanged.
  2. **`000_lockdown.sql`'s revoke list was missing `MAINTAIN`.** PostgreSQL 15 added a `MAINTAIN` table privilege (VACUUM/ANALYZE/CLUSTER/REINDEX/etc.) that didn't exist when the older revoke-list idiom in Appendix A.1 was written, so `anon`/`authenticated` kept it after 000_lockdown.sql ran. Caught by the corrected audit above (query 3 returned two `MAINTAIN` rows instead of zero). Since `000_lockdown.sql` was already applied and its checksum locked, fixed with a follow-up file `migrations/000a_lockdown_maintain.sql` (`revoke maintain on public.recipes from anon, authenticated;`) rather than editing the applied file. Re-ran the audit: zero rows.
- Confirmed findings from 2026-09-10 verification (§1.4) reproduced exactly: 4 pre-lockdown policies (`Allow anon recipe reads` SELECT, `Allow anon recipe updates` UPDATE, `Allow public inserts on recipes` INSERT, `Allow public read access to recipes` SELECT), no DELETE policy, and `anon`/`authenticated` holding every table privilege on `recipes` via `relacl` (`arwdDxtm`) before this migration — confirming anon hard DELETE was never possible only because no DELETE *policy* existed under RLS-off PostgREST semantics, while the raw grant existed underneath.
- Notes for next phase: Phase 1 (repo restructure & tooling) is next. `scripts/lib/env.mjs` bakes in the known public constants (`SUPABASE_URL`, `SUPABASE_PROJECT_REF`, `SUPABASE_PUBLISHABLE_KEY`, `CLOUDFLARE_ACCOUNT_ID`, `AI_MODEL`, `GEMINI_MODEL`) so `.env.local` only needs to carry owner-provided secrets/decisions — Phase 1's `wrangler.toml` `[vars]` should still hardcode these same values per the spec, since Workers can't read `.env.local`.

---

## Phase 1 — Repository restructure & tooling — DONE
- Date: 2026-09-10
- Branch / PR: phase-1-tooling / [#3](https://github.com/dugguboy2015-sudo/recipe-book/pull/3) (merged c085160)
- Migrations applied: none (no schema changes this phase)
- Backup: not required (no data-affecting changes)
- Acceptance:
  - [x] `npm run check` passes locally and in CI (GitHub Actions `check` job, added this phase, ran for the first time on the PR and passed)
  - [x] Preview: all three pages render and load 30 recipes; no CSP violations in the console
  - [x] Response headers include the CSP; the supabase-js `<script>` carries `integrity` (sha384, computed over the exact bytes served for `@supabase/supabase-js@2.116.0`'s UMD build)
  - [x] No `?v=` query strings remain; no `style="` attributes in `public/` (both instances were the same modal-footer rule, moved to a `.modal-footer` class)
  - [x] Production smoke passes after merge
- Preview smoke: PASS https://9627906a.recipe-book-9eo.pages.dev (all 6 checks)   Production smoke: PASS https://recipe-book-9eo.pages.dev (all 6 checks; also manually verified in-browser on all three pages — 30 recipes render, zero console errors)
- Deviations from spec:
  1. **Local `wrangler pages dev` cannot run the `AI` binding.** It needs to proxy to Workers AI in "remote" mode even for local dev, which requires broader account permission (Workers Scripts) than our custom Cloudflare token grants (Pages Edit, Workers AI Edit, Account Settings Read, Turnstile Edit, User Details Read, Memberships Read). `wrangler whoami` confirms the token itself authenticates fine and resolves the right account; the remote-binding session specifically is refused. Worked around it for this phase by verifying everything against the pushed Cloudflare Pages preview deployment instead (which is what the spec's own SOP does anyway). This will need revisiting in Phase 9 when Functions actually call the AI binding — either widen the token's scope or find another local-dev path for that one endpoint.
  2. **`scripts/smoke.mjs`'s write-guard check accepts 405 as well as 401/403/404.** Cloudflare Pages Functions returns `405 Method Not Allowed` (not `404`) for `POST /api/recipes` while no function file exists at that route yet, discovered running smoke against the phase-1-tooling preview. Not a bug in our code — Cloudflare's own Functions router — so the check was broadened rather than "fixed".
  3. Added `tests/lib/env.test.js`, one test file covering `scripts/lib/env.mjs` (KEY=VALUE parsing, missing-required-key errors, the known-defaults merge). Not in the original Phase 1 task list, but `npm test` / CI need at least one test file to pass, and Appendix G's fuller suite isn't due until Phase 4 (once `recipe-rules.js` exists). Kept intentionally small — just covers code already written this phase.
- Also noted (not a deviation, just an environment quirk worth recording): a fresh `npm install` on this Windows machine hit the known npm/cli#4828 optional-dependency bug (`Cannot find module @rollup/rollup-win32-x64-msvc`) after `npm ci`-equivalent installs. Fixed locally with `npm install --no-save @rollup/rollup-win32-x64-msvc`; doesn't affect `package-lock.json` and doesn't affect Linux CI, which resolves its own platform's optional binary normally.
- Notes for next phase: Phase 2 (schema hardening & health columns) is next. Migrations 001–008 apply against the now-locked-down database; nothing in `public/js` reads the new columns yet, so no expand/contract risk. `scripts/sync-config.mjs` (2.5) will need `public/config/` to exist — it doesn't yet, `scripts/sync-config.mjs` should create it.

---

## Phase 2 — Schema hardening & health columns (expand-only) — DONE
- Date: 2026-09-11
- Branch / PR: phase-2-schema / [#5](https://github.com/dugguboy2015-sudo/recipe-book/pull/5) (merged 86fe961)
- Migrations applied: 001_constraints.sql, 002_cuisines.sql, 003_slug.sql, 004_meal_types.sql, 005_audit_and_generations.sql, 005a_recipe_audit_log_seq_fix.sql, 006_health_columns.sql, 007_read_views.sql, 008_default_privileges.sql
- Backup: backups/2026-09-10T23-07-46-476Z (32 rows) — taken before this phase's migrations
- Acceptance:
  - [x] `schema_migrations` lists 000–008 (plus the two follow-up fixes 000a, 005a)
  - [x] Every A.9 expectation holds (all 10 checks; see below); corrected A.0 audit returns zero rows
  - [x] `is_protein_smart` exists and is `false` for every current row (confirmed: `count(*) filter (where is_protein_smart) = 0`)
  - [x] The live site still reads correctly (production smoke + manual browser check, zero console errors)
- Preview smoke: PASS https://33e1187c.recipe-book-9eo.pages.dev   Production smoke: PASS https://recipe-book-9eo.pages.dev
- A.9 verification results:
  1. Dietary columns: `is_egg_free`/`is_vegetarian`/`contains_dairy` all default `null`/`NOT NULL`; `is_deleted` default `false`/`NOT NULL`; `cuisine` `NOT NULL` — matches exactly.
  2. Trigger `recipes_set_updated_at` present.
  3. Zero rows with `total_time_minutes` < parts.
  4. Cuisine distribution matches exactly: Chaat 3, Fusion 6, Gujarati 1, Indo-Chinese 3, Maharashtrian 9, North Indian 4, Other 2, Rajasthani 2, South Indian 2 (32 total).
  5. Zero rows with missing slug.
  6. Meal-type counts (non-deleted) match §1.4's tag vocabulary with the Snack→Snacks and Packed Lunch Friendly→Packed Lunch renames: Breakfast 9, Dessert 5, Dinner 13, Lunch 18, Packed Lunch 8, Snacks 16.
  7. `similar_recipes('kanda poha', 3)` (called via service-role REST RPC, since the Management API's read-only role can't execute it — same pattern as the A.0 audit fix) returns Kanda Poha, score 1.0.
  8. `recipe_stats`: `total: 30, vegetarian: 30, egg_free: 30, dairy_free: 0, protein_smart: 0, top_cuisine: Maharashtrian, top_cuisine_count: 9`.
  9. Corrected A.0 audit (RLS enabled, security_invoker views, non-select anon/authenticated table privileges via relacl): all three return zero rows.
  10. `count(*) filter (where is_protein_smart)` = 0, as expected.
- Deviations from spec:
  1. **`scripts/migrate.mjs`'s checksum guard was broken by Windows line-ending normalization.** It hashed migration files' raw bytes, but `core.autocrlf=true` (this machine's git config) rewrites LF→CRLF on every checkout. The very first `git switch` after Phase 0 changed `000_lockdown.sql`'s on-disk bytes without changing its content, and `migrate.mjs` refused to proceed ("Applied migration has changed on disk"). Fixed by normalizing `\r\n`→`\n` before hashing in both the already-applied-check and the new-file digest — this made the two Phase 0 files re-match immediately (they were LF when first hashed), no data or `schema_migrations` changes needed.
  2. **`001_constraints.sql`'s `recipes_serves_range` check (1–50) failed** against the pre-existing junk soft-deleted row id 39 ("Hdjd", `serves: 7022`) — the same row `002_cuisines.sql` already special-cases for its junk cuisine/tags. Added `update ... set serves = 4 where id = 39 and is_deleted and serves > 50` immediately before the constraint, scoped tightly to that one known-junk row.
  3. **`recipe_audit_log`'s identity-column sequence was exposed to `anon`/`authenticated`** (SELECT/UPDATE/USAGE) by this Supabase project's default grant behaviour for sequences — the same phenomenon Phase 0 found on tables (`public.recipes` originally had anon holding every privilege), just for sequences, and only affecting objects created before `008_default_privileges.sql` locks down future defaults. Found by extending the standing A.0 audit to also check `pg_class.relkind='S'`. Fixed with `migrations/005a_recipe_audit_log_seq_fix.sql`. This relkind='S' check is now part of the standing audit for every future phase that creates a table with a `generated ... as identity` column.
- Notes for next phase: Phase 3 (structured ingredients, units & nutrition backfill) is next. `scripts/fixtures/recipes.snapshot.json` (32 rows, timestamps stripped, taken from the pre-migration backup) and `scripts/fixtures/slugs.json` are ready for the ingredient-backfill authoring work. Remember to check every new identity-column table's backing sequence against `anon`/`authenticated` after applying, until the pattern is proven to no longer occur (008's default-privilege revoke should prevent it for anything created from Phase 3 onward, but verify rather than assume).

---

## Phase 3 — Structured ingredients, units & nutrition backfill — DONE
- Date: 2026-09-11
- Branch / PR: phase-3-ingredients / [#7](https://github.com/dugguboy2015-sudo/recipe-book/pull/7) (merged d2998ed)
- Migrations applied: 009_ingredients_schema.sql, 010_ingredients_backfill.sql, 011_nutrition_backfill.sql, 012_derived_flags.sql, 012a_contains_dairy_correction.sql
- Backup: backups/2026-09-10T23-22-16-151Z (before 009) and backups/2026-09-10T23-36-46-148Z (before 010-012a), both 32 rows
- Acceptance:
  - [x] `validate-backfill.mjs` passes with zero structural errors (unit/category validity, no shared aliases, full line coverage per recipe)
  - [x] Every live recipe has structured ingredients and a spice level, **except id 2** (documented data-loss case below); every live recipe with ingredients has sugars, saturates and salt
  - [x] The dietary derivation matches for all 29 recipes with ingredients (14 `contains_dairy` mismatches were found, logged, and corrected by 012a; `is_vegetarian`/`is_egg_free` had zero mismatches)
  - [x] The protein-smart share of the collection is recorded: 0/30 (see below)
- Preview smoke: PASS (verified locally against production REST API before pushing, since this phase touches no served files)   Production smoke: PASS https://recipe-book-9eo.pages.dev (all 6 checks; recipes page manually verified in-browser, zero console errors, structured ingredients read correctly via `recipe_ingredients` join)
- **Data-loss recipes** (pre-existing corruption from the known edit-flattening bug, BUG-5/BUG-10, not something this migration caused):
  - **Id 2, "Aloo Paratha Roll with Ketchup"**: both `ingredients` and `steps` are literally `"asDfasdf"`/`"asdfasdf"` in the live database — nothing to recover from. Skipped rather than fabricated: this is the one live recipe with zero `recipe_ingredients` rows and no nutrition backfill entry. **Needs the owner to re-enter this recipe by hand** (or delete it) — flagging here since it's the only unresolved data-quality issue left from Phase 3.
  - **Id 4, "Basundi"**: stored `ingredients` was `[]`, but the method text fully describes the dish (milk, sugar, saffron-soaked milk, cardamom, pistachios, almonds, cashews). Reconstructed a plausible ingredient list with judgment-based quantities (`scripts/data/ingredient-overrides.mjs`, `RECONSTRUCTED_GROUPS[4]`) — these are reasonable estimates, not recovered originals. Worth a quick owner glance.
  - **Id 12, "Kothimbir Vadi"**: stored `ingredients` only had the tempering group; the method describes a full batter (coriander, gram flour, rice flour, ginger-garlic, chillies, spices, sugar, lemon) that was missing entirely. Reconstructed the missing "For the batter" group the same way, prepended before the surviving tempering group.
  - **Id 27, "Shrikhand"**: same situation as Basundi (`ingredients` was `[]`, method intact — strained yoghurt, sugar, saffron milk, cardamom, nuts). Reconstructed similarly.
- **Contains_dairy correction** (012a): recipes 3, 5, 7, 11, 12, 13, 14, 15, 18, 19, 25, 26, 28, 29 were changed from `contains_dairy: true` to `false` — this is exactly the bug described in improvement_plan.md §1.4 ("all 30 live recipes are marked contains_dairy: true, which can't all be right, e.g. Lemon Rice, Schezwan Noodles"); both of those named examples are in the corrected list. Not a silent change — logged here and in the migration's own comment.
- Protein-smart share: 0/30. None of the existing recipes clear J.2's `protein_g * 4 >= 0.20 * calories_kcal` bar even where `protein_g >= 15` (Chilli Paneer, Paneer Tikka Wrap, Paneer Tikka Paratha Roll all fail this specific check — their calories are too high relative to protein). Expected: this collection predates the household health goals: Phase 9's AI generation is what drives new recipes toward the 60% target, not a retroactive rewrite of existing ones.
- Deviations from spec:
  1. The three items above (data-loss reconstruction/skip) aren't strictly "deviations" from a spec instruction, but are deviations from a clean backfill — recorded here per §0.5's spirit of logging anything the spec's normative appendices didn't anticipate.
  2. Extended the standing A.0 RLS/grants audit (see Phase 0's deviation entry) to also check `pg_class.relkind = 'S'` for stray `anon`/`authenticated` sequence grants on every new table. Came back clean this time — confirms `008_default_privileges.sql`'s revoke is working for objects created from Phase 3 onward, unlike `recipe_audit_log` in Phase 2 (created in 005, before 008 took effect).
  3. One real migration bug caught and fixed before it ever committed: `009`'s `group_name` has a 60-char check constraint, but the live source data for Kothimbir Vadi's tempering group is `"For the tempering (optional, for extra flavour after steaming)"` (64 chars). `scripts/build-ingredients-backfill.mjs` now has a `fitGroupName()` helper that strips a trailing parenthetical before hard-truncating; no group name over 60 chars remains.
- Notes for next phase: Phase 4 (front-end modularisation) is next. The ingredient dictionary (`scripts/data/ingredient-dictionary.mjs`) and backfill scripts are one-time authoring tools, not part of the standing `npm run check` pipeline — they're not needed again until a future recipe needs re-backfilling. `public/js/shared/units.js` (cup/spoon display formatting, Appendix C.2) will need to agree with the `units` table's `to_base` values used here (cup=240ml, tbsp=15ml, tsp=5ml).

---

## Phase 4 — Front-end modularisation (behaviour-preserving) — DONE
- Date: 2026-09-11
- Branch / PR: phase-4-modules / [#9](https://github.com/dugguboy2015-sudo/recipe-book/pull/9) (merged a3468fd)
- Migrations applied: none (front-end only)
- Backup: not required (no data-affecting changes)
- Acceptance:
  - [x] `app.js` is gone; no module exceeds 300 lines (largest is `shared/recipe-rules.js` at ~300, `pages/recipes.js` at ~220)
  - [x] Every parity-checklist line (43 items, `docs/parity-checklist.md`) verified on preview via the browser tool: dashboard stats/recent, detail modal open/close, recipes page search/filters/pagination/clear, add/edit form fill/validate/focus/cancel, delete-confirm modal, full planner flow (select/assign/remove/reset/persist). Zero console errors throughout.
  - [x] `npm run check` passes; Appendix G.1 tests exist and pass (46 tests) plus 8 query-builder tests, 57 total
  - [x] Network log on preview shows no `is_deleted,deleted_at` probe query on page load — `checkSoftDeleteSupport()` is gone entirely
- Preview smoke: PASS https://eb309462.recipe-book-9eo.pages.dev (all 6 checks + full manual walkthrough)   Production smoke: PASS https://recipe-book-9eo.pages.dev (all 6 checks; manually re-verified in-browser, zero console errors)
- Deviations from spec:
  1. Two small consolidations beyond the one explicitly permitted (deleting `checkSoftDeleteSupport`), both because the refactor unavoidably passed through this code and the visible result is provably identical:
     - Dashboard previously fetched the recipe list twice (an initial fetch to render "recently added", then `loadDashboard()` fetching everything again to compute stats and re-render the same 3 cards). Now computed from one query.
     - The two byte-identical recipe-detail-modal markup blocks in `index.html`/`recipes.html` are gone, per task 4.4's own instruction to render the markup once from `components/recipe-modal.js`.
  2. `config.js` doesn't yet include `TURNSTILE_SITE_KEY` (the target layout in §4's file list mentions it "by hostname"). Nothing consumes Turnstile until Phase 6, so it's added then rather than as unused dead config now.
  3. Confirmed (not new, but now blocking local iteration directly): `wrangler pages dev` cannot serve *any* route locally while `wrangler.toml` declares the `AI` binding — it always tries to establish the remote-binding session first, regardless of whether the requested route touches AI, and there's no local runtime for that binding to fall back to. This isn't a flag-fixable wrangler limitation; verified this phase entirely against the Cloudflare Pages preview deployment instead. Still on the list to address by Phase 9 (wider token scope, or accept preview-only local iteration for AI-adjacent work).
- Notes for next phase: Phase 5 (design system & app shell) is next. `public/js/shared/recipe-rules.js` is fully implemented (not just slugify/escapeHtml) and tested, so Phases 6-9 can build directly on `normalizeRecipeInput`, `dietaryWarnings`, the ingredient/step text round-trip, and `reconcileTimes` without redoing that work. `public/js/components/recipe-form.js` still writes straight to Supabase with the publishable key — Phase 6 replaces that internals only, the module's public `openAdd`/`openEdit` shape shouldn't need to change.

---

## Phase 5 — Design system & app shell — DONE
- Date: 2026-09-11
- Branch / PR: phase-5-design / [#11](https://github.com/dugguboy2015-sudo/recipe-book/pull/11) (merged 921e04b)
- Migrations applied: none (front-end only)
- Backup: not required (no data-affecting changes)
- Acceptance:
  - [x] `npm run check` passes, including `lint-css.mjs` and `contrast.mjs` (every token pair actually used together passes its WCAG threshold — 4.5:1 text, 3:1 the one UI-boundary case checked — in both themes)
  - [x] The style guide (`public/styleguide.html`) shows every Appendix L.3 component in every applicable state (default/hover/focus/disabled/loading/error as relevant), both themes via a toggle; not linked from the app nav
  - [x] All three pages use only the new CSS (`tokens/base/components/pages.css`); `styles.css` deleted. No visual regressions against the Phase 4 parity checklist — re-walked interactively on the preview (search/filters/pagination/clear, add/edit form validate+focus+cancel, delete-confirm, full planner flow) and confirmed on production afterward
  - [x] Keyboard: every recipe card opens with Enter (title is a real `<button>` stretched over the card, UX-1); all three dialogs trap focus and restore it to the opener on close (native `<dialog>`, UX-2); Escape closes
- Preview smoke: PASS (final preview https://e08f21ae.recipe-book-9eo.pages.dev, all 6 checks + full interactive walkthrough)   Production smoke: PASS https://recipe-book-9eo.pages.dev (all 6 checks; confirmed in-browser via computed styles that all 4 stylesheets load and apply correctly — dark theme background `rgb(27,33,48)`, correct nav styling)
- Deviations from spec:
  1. **Dialog centering bug, found and fixed mid-phase.** `base.css`'s universal reset (`* { margin: 0 }`) overrode the browser's built-in `dialog:modal { margin: auto }` centering rule (a bare type selector like `dialog` beats the universal selector `*` on specificity, regardless of source order) — every dialog rendered flush at the viewport's top-left corner. This silently broke `dialog.js`'s backdrop-click-to-close too, since the dialog's bounding box then covered the whole click-testable area. One-line fix in `components.css`: `dialog { margin: auto; }`. Found by deliberately testing backdrop-click against the preview rather than assuming the CSS reset was safe.
  2. **CSP for `styleguide.html`: an approach that doesn't work, and the fix.** First attempt added a per-path `_headers` block relaxing `style-src` to `unsafe-inline` for `/styleguide.html` only, to allow its inline colour-swatch `style=""` attributes. Confirmed via the preview's console that this **does not work**: when multiple `_headers` blocks match a request and each sets `Content-Security-Policy`, the browser receives both headers and enforces their *intersection* (per the CSP spec, a resource must satisfy every `Content-Security-Policy` header delivered, not just the most specific one) — so the strict `/*` policy kept applying regardless. Removed that `_headers` block entirely (net no diff against Phase 1's file) and instead made the page itself CSP-compliant: every swatch became a class in a new `public/css/styleguide.css`, and the inline demo `<script type="module">` moved to `public/js/styleguide.js`. Worth remembering for any future per-path CSP idea: `_headers` can only ever make a path's policy *more* restrictive by adding a block, never less.
  3. **`config.js` doesn't yet carry `TURNSTILE_SITE_KEY`**, despite the target file layout mentioning it "by hostname" as of this phase. Nothing consumes Turnstile until Phase 6; added then instead of as unused config now.
  4. Recipe id 2 (the Phase-3-documented data-loss recipe) shows "234g protein per serving" on its card — its pre-existing corrupted nutrition value, now surfaced for the first time because v2 cards display protein per serving. No sanity-clamping was added to the display layer (that would be speculative validation outside this phase's scope); recorded as one more reason the owner should re-enter that recipe by hand (see Phase 3's entry above for the rest).
- Notes for next phase: Phase 6 (write API through Pages Functions) is next. `components/recipe-form.js` currently writes straight to Supabase with the publishable key (blocked by the Phase 0 lockdown) — Phase 6 replaces those two `client.from('recipes')` calls with `fetch('/api/recipes', …)` calls; the `open`/`close`/`openAdd`/`openEdit` public shape and the native-dialog wiring shouldn't need to change. `components/dialog.js` is now the standing pattern for any future modal (Phase 7's structured-ingredient editor will want one).

---

## Phase 6 — Write API through Cloudflare Pages Functions — DONE
- Date: 2026-09-11
- Branch / PR: phase-6-write-api / [#13](https://github.com/dugguboy2015-sudo/recipe-book/pull/13) (merged 2debad4)
- Migrations applied: 013_save_recipe_rpc.sql
- Backup: not required (no destructive data change; the RPC only adds capability)
- Acceptance:
  - [ ] Local `--write` smoke passes end to end — **blocked**, not run. `wrangler pages dev` cannot start at all on this machine (the `AI` binding always tries to establish a remote-binding session before serving any route, per the Phase 1 finding, and our Cloudflare token doesn't have the broader Workers Scripts permission that needs). The `--write` mode itself is written correctly per spec in `scripts/smoke.mjs` for whenever this is resolved.
  - [x] Preview read-only smoke passes, including the 403 checks (7/7, both on preview and again on production after merge — one transient MIME/405 blip immediately after each deploy, gone on retry, matching the same post-deploy edge-propagation pattern seen after Phase 5's merge)
  - [x] Function tests cover every row of the pipeline and operations tables (`tests/functions/*.test.js`, 17 tests: every pipeline failure in order, create/update/delete/restore success, 23505→409, zero-row update→404, PT409→409 with `current`, PT400 dietary_mismatch→400 with evidence messages, and audit-written-on-success-never-on-failure)
  - [x] `git grep -n SUPABASE_SECRET_KEY public/` finds nothing
  - [ ] After merge, adding/editing/deleting/undoing works on production, verified in a real browser — **delete and undo confirmed working**; **create's Turnstile step could not be completed by this automated browser**, and that's Turnstile doing its job (see deviation below), not a gap in the code. Edit is intentionally disabled this phase.
  - [x] Edit is disabled on every recipe with the exact notice text; add (name-only ingredient rows), delete and undo work
- Preview smoke: PASS (7/7)   Production smoke: PASS (7/7, after one transient retry)
- Deviations from spec / findings while building this:
  1. **`check-secrets.mjs` had a real gap**: it treated every literal value in `.env.local` as a secret, which flagged `TURNSTILE_SITE_KEY` — explicitly public per §3.2 and required in committed `config.js` for Turnstile to work at all. Added an exclusion list for vars documented as deliberately public.
  2. **`normalizeRecipeInput` / B.4 payload shape mismatch**: the shared validator's ingredient-count check looks for an `ingredients` key on the object it validates, but the B.4 wire format sends `ingredients` as a sibling of `recipe`, not nested inside it. Every create failed strict validation with "Add at least one ingredient" regardless of what was actually sent, until both endpoint call sites were fixed to validate `{ ...body.recipe, ingredients: body.ingredients }`.
  3. **`set-secrets.mjs` never actually ran until now.** Task 1.7 said to run it once in Phase 1; it wasn't, so `SUPABASE_SECRET_KEY`, `TURNSTILE_SECRET_KEY`, `IP_HASH_SALT` and `GEMINI_API_KEY` were never pushed to Cloudflare Pages — confirmed via `wrangler pages secret list` showing zero secrets on both environments right before this phase. Also found `execFileSync('npx', …)` fails on Windows (ENOENT, then EINVAL when manually resolved to `npx.cmd`) since `npx` is a `.cmd` wrapper needing shell resolution that `execFileSync` doesn't provide by default; fixed with `shell: true`. Ran it now — all four secrets confirmed present on both `production` and `preview`.
  4. **Turnstile could not be completed by the automated browser used for verification**, on *both* the preview and production domains. My first hypothesis (preview's random-hash subdomain isn't registered with the widget) turned out to be wrong — production showed the exact same behaviour: the widget renders its DOM (hidden `cf-turnstile-response` input) but never produces a challenge iframe, `getToken()`'s 30s timeout fires, and the UI correctly shows "We couldn't confirm you're not a bot. Try saving again." — the exact copy from task 6.4's table. This is Turnstile's managed mode detecting automated-browser signals and declining to issue a token, which is precisely what it's designed to do; it is not a defect in this code. Confirmed no `__smoke__` recipe was created (Turnstile blocked the request before it ever reached the API). **The create/save success path is verified via the 82 automated tests (scripted Turnstile responses) and needs one real manual test by a human in an ordinary browser to close the loop** — delete and undo don't need a fresh Turnstile pass to *observe* correctly here since they were exercised against already-existing real recipes and both worked (soft delete + the 8s Undo snackbar restored the recipe correctly).
- Notes for next phase: Phase 7 (recipe form, detail view, servings & cook mode) is next. It removes the Phase 6 edit guard and replaces the name-only ingredient textarea with the full structured editor (Appendix 7.1); `lib/api.js`'s `updateRecipe()` is already correct and tested (via the function tests) even though nothing calls it yet. `functions/api/recipes/[id].js`'s PATCH handler already fully implements partial updates, edit-conflict, and dietary-mismatch mapping — Phase 7 just needs to wire the UI to it.

---

## Phase 7 — Recipe form, detail view, servings & cook mode — DONE
- Date: 2026-09-11
- Branch / PR: phase-7-recipe-ui / [#15](https://github.com/dugguboy2015-sudo/recipe-book/pull/15) (merged 5ebca4c)
- Migrations applied: 014_tags_contract.sql (applied after this phase's code was live in production, per the spec's sequencing)
- Backup: `backups/2026-09-11T04-29-24-868Z` (32 rows), taken immediately before 014
- Acceptance:
  - [x] Opening recipe 1, editing and saving without changes leaves `recipe_ingredients` identical — verified at the payload-shape level (`recipeIngredientsToPayload` is unit-tested as idempotent on a fetched-shape fixture) and by inspection in a real browser: the ingredient editor's `setValue()` from `fetchRecipeIngredients` reproduces the exact same groups, order, quantities and units recipe 1 already has. The actual PATCH round trip against the live RPC couldn't be exercised end-to-end (see Turnstile finding below), so this is verified short of an actual save.
  - [x] Recipe 1 at 4 → 6 servings shows exactly "1.5 cup → 2¼ cup" for whole wheat flour; pinch/to-taste rows unchanged — confirmed live on the production detail view (`½ tsp Salt` → `¾ tsp`, `to taste Salt, to taste` unchanged)
  - [x] A dietary contradiction is refused with a clear field message — the household-confirm dialog fires correctly client-side ("This household is vegetarian and egg-free. Save anyway?") for a non-vegetarian/non-egg-free answer; the server-side `dietary_mismatch` (PT400) mapping was already covered by Phase 6's function tests and is unchanged
  - [x] Every writable column settable from the form — all of `WRITABLE_FIELDS` (Appendix C) plus the B.4 ingredients array have a field in the restructured 7.3 sections
  - [x] Cook mode keeps the screen awake where supported and is keyboard-operable — `navigator.wakeLock.request('screen')` is requested and its rejection (denied/unsupported) is caught and silently ignored; Previous/Next/Escape verified via real keyboard events in the browser tool
  - [x] 014 applied; meal-type words no longer appear in `tags` — verified: 0 rows match `"(Breakfast|Lunch|Dinner|Snack|Snacks|Dessert|Packed Lunch|Packed Lunch Friendly)"` in `tags` after the migration, row count unchanged (32), and unrelated tags (e.g. "Side Dish") were left untouched
- Preview smoke: PASS https://phase-7-recipe-ui.recipe-book-9eo.pages.dev (7/7, plus an extensive manual walkthrough — see findings below)   Production smoke: PASS https://recipe-book-9eo.pages.dev (7/7, before and again after the 014 migration)
- Deviations from spec / findings while building this:
  1. **Two real bugs found via browser-testing this phase's new dialogs, both fixed before merge:**
     - `ingredient-editor.js` renders synchronously during construction (its first `render()` call happens inside `createIngredientEditor()` itself) and that render fires `onChange`. `recipe-form.js` passed `applyDietarySuggestion` as `onChange` where the callback closed over the `const ingredientEditor` binding that was still being assigned — a `ReferenceError: Cannot access 'ingredientEditor' before initialization` on every single page load, caught immediately via the browser tool's console. Fixed with a `let ingredientEditorRef` initialized to `null` before construction, assigned right after.
     - `dialog.js` (Phase 5 code, unchanged by this phase until now): its backdrop-click-to-close compared the click event's `clientX`/`clientY` against the dialog's bounding rect. A **keyboard-activated click** (pressing Enter on a focused button, or Space on a focused checkbox — a core, expected way to operate any of this app's forms) synthesizes a `MouseEvent` with `clientX`/`clientY` both `0`, which that coordinate check always misread as "outside the dialog," closing it immediately. This meant pressing Enter to submit the add-recipe form, or Space on any checkbox inside it, silently closed the whole dialog instead of doing anything — found while scripting the household-confirm flow, where the "Go back" click closed *both* the confirm dialog and the add-recipe form behind it. Fixed to check `event.target === dialog` instead (the standard way to detect a genuine `::backdrop` click, since backdrop clicks report the dialog itself as `event.target`; clicks on any descendant, real or keyboard-synthesized, never do). This is a general accessibility fix for every dialog in the app, not just this phase's new ones.
  2. **The dietary auto-suggestion ("Based on the ingredients: …") is correctly dormant for every existing ingredient right now.** It requires each named row to resolve to a known ingredient (or be a brand-new one, whose flags are fresh) *and*, for an existing one, that its `status` be `'reviewed'` — but `select count(*) from ingredients where status = 'reviewed'` is currently `0` of 97 (the Phase 3 backfill inserts everything as `'unreviewed'`, and no review tooling exists yet). Verified this is the gate working as designed, not a bug: the suggestion does fire correctly when every row is a brand-new ingredient (tested live — a recipe with only a new "zzz-test" ingredient showed and preselected the suggestion correctly), it just never fires yet for recipes built from the existing catalog. This will start working automatically once ingredients start getting marked reviewed (out of scope here — no review UI was asked for).
  3. **Full create/update submissions still can't be completed by this automated browser**, for the same reason as Phase 6: Turnstile correctly detects the automated session and never issues a token, so `getToken()`'s 30 s timeout fires and the UI shows the expected "We couldn't confirm you're not a bot" message — confirmed this is Turnstile working as intended, not a defect, by checking no `__smoke__`-style row was created. Everything up to that point (field population, ingredient search/selection/paste, method editor, dietary suggestion, household confirm open/cancel, meal-type↔packed-lunch conditional, time auto-fill, cuisine ordering) was verified directly against live production data in a real browser. **Recommend one manual create and one manual edit in an ordinary browser to close the loop on the actual save path**, same recommendation as Phase 6.
  4. `planner.js`'s day/slot/localStorage logic was extracted into a new shared `lib/planner-store.js` (not called for explicitly by the task list) so the new "Add to planner" action on the recipe detail view could write the exact same shape the planner page reads, rather than duplicating that logic a second time. `planner.js` itself is behaviour-identical, just re-wired to import from the shared module.
  5. Cook mode, the servings stepper's live scaling, and the %RI traffic lights all needed a CSP-safe way to set a dynamic width (strict `style-src 'self'` forbids `style="width: …"`, inline or script-set) — added a small `.w-pct-0` … `.w-pct-100` (5% steps) utility-class set to `components.css` rather than reaching for an inline style.
- Notes for next phase: Phase 8 (correctness & performance) is next. `queries.js` already has `fetchRecipesList` accepting `mealTypes`/`proteinSmart`/`nutFree`/`spiceMax` filters (added this phase for 7.6's UI) — Phase 8's `searchRecipes`/URL-state work can build on that filter shape directly rather than reinventing it. The dashboard's full-table scan (BUG-1) and the planner's `.limit(50)` preload (BUG-6) are both still exactly as they were.

---

## Phase 8 — Correctness & performance — DONE
- Date: 2026-09-11
- Branch / PR: phase-8-fixes / [#17](https://github.com/dugguboy2015-sudo/recipe-book/pull/17) (merged 348209f)
- Migrations applied: none — `recipe_stats` (already including `protein_smart`), `cuisine_counts`, `tag_counts` and `planner_candidates` all already existed from Phases 2/3's migrations, unused by the client until now
- Backup: not required (no data-affecting changes)
- Acceptance:
  - [x] Dashboard load makes exactly 2 Supabase requests — verified via `performance.getEntriesByType('resource')` on the preview: `recipe_stats?select=*` and one bounded `recipes...order=created_at.desc&limit=3`, nothing else
  - [x] Recipes page load makes at most 4 requests, none unbounded — verified the same way: `cuisine_counts`, `tag_counts`, a `count`-only query, and one `limit=12` data query
  - [x] Blocking `*.supabase.co` shows the error state with Retry, never "No recipes match" — verified by patching `window.fetch` to reject every `supabase.co` call: the recipes page showed "Couldn't load recipes. Check your connection." with a Retry button that recovered the list once the network was restored
  - [x] Planner search finds a recipe that sorts after the old preload cutoff — this dataset only has 30 recipes (not enough to need a real `__smoke__zzz` fixture), so verified in spirit: "Vada Pav" (alphabetical position 28 of 30, well past both the old `.limit(50)` and the new 12-item initial view) is found via the search box and can be assigned to a planner slot
  - [x] Refresh keeps filters and page — verified: applying `cuisine=Fusion` + the Vegetarian chip updates the URL live via `history.replaceState`, and loading that URL fresh hydrates both the filter UI and the result set correctly
- Preview smoke: PASS https://phase-8-fixes.recipe-book-9eo.pages.dev (7/7)   Production smoke: PASS https://recipe-book-9eo.pages.dev (7/7)
- Deviations from spec / findings while building this:
  1. **No new migrations were needed.** Re-reading Appendix A.11/A.13, `007_read_views.sql` (Phase 2) already defines `recipe_stats` (with `protein_smart` included from the start), `cuisine_counts` and `tag_counts`, and `012_derived_flags.sql` (Phase 3) already defines `planner_candidates` with exactly the columns Appendix K.3 asks for. This phase was purely a client-side rewrite to actually *use* those existing read models instead of the ad-hoc full-table queries the pages had been running.
  2. **8.5's `searchRecipes()`** takes `dietary` as one nested object (`{vegetarian, eggFree, dairyFree, proteinSmart, nutFree, spiceMax}`) rather than flat top-level keys, to keep the recipe-vs-dietary filter boundary explicit; `recipes.js`'s flat `state.filters` maps into that shape at the call site. The planner calls it with only `{term}` — it doesn't expose the dietary/cuisine/tag filters in its UI, only a name search, per the existing planner design.
  3. This dataset (30 live recipes) is too small to construct a literal "beyond position 50" fixture the way 8's acceptance text implies (create a `__smoke__zzz` row, find it, clean up) — verified the underlying fix (server-side search with no preload cutoff) using a real recipe that already sorts past both the old and new initial-view boundaries instead. The mechanism is identical either way: `searchRecipes()` queries `.ilike` server-side over the full table, unbounded by any client-side preload.
- Notes for next phase: Phase 9 (AI generation endpoint) is next. It's the first phase touching Workers AI / `env.AI`, which is exactly the binding that blocks `wrangler pages dev` from starting locally at all (the standing limitation recorded since Phase 1) — this needs to be resolved or worked around (preview-only iteration, or a broader Cloudflare API token) before Phase 9 can be developed and tested efficiently. `queries.js`'s `searchRecipes()` and the `{ok, data, error}` contract are now the standing pattern for any new list-returning query Phase 9 or later phases add.

---

## Phase 9 — AI generation endpoint — DONE (partial verification — see deviations)
- Date: 2026-09-11
- Branch / PR: phase-9-generate / [#19](https://github.com/dugguboy2015-sudo/recipe-book/pull/19) (merged c1e8cd8)
- Migrations applied: none — `recipe_generations` (with the full v2 column set: `goal`, `effective_goal`, `protein_smart`, `kind`), `similar_recipes`, `match_ingredient` and `ingredient_usage` all already existed from Phases 2/3/6; this phase is purely the Functions layer using them
- Backup: not required (no data-affecting changes; `scripts/extract-example.mjs` only reads)
- Acceptance:
  - [ ] `docs/generation-eval.md` (the 11-case eval) — **not produced.** `scripts/eval-generate.mjs` is written exactly per spec (11 cases, writes the markdown table, computes median latency) but needs `npm run dev` to run, which has never worked on this machine (the `[ai]` binding's local-dev limitation, standing since Phase 1). No real Workers AI or Gemini call has been made this phase.
  - [ ] Median latency under 45s for cases 1-5/9-11 — unverified, same reason
  - [x] The quota endpoint's numbers match the `recipe_generations` counts — verified against the **real production database**: `GET /api/recipes/generate/quota` returned `{"remainingToday":18,"remainingForYou":5,...}` against a `recipe_generations` table with zero rows for today, matching `GEN_GLOBAL_DAILY=18`/`GEN_PER_IP_DAILY=5` exactly
  - [x] Tests pass; no real model calls in CI — 34 new tests (230 total), all using scripted/mocked `env.AI.run` and stubbed `fetch`, covering pipeline order and short-circuits, `response` as object/string/fenced-JSON/garbage, the parse-retry-to-Gemini and parse-retry-to-Workers-AI paths, the content-validation retry (household rule / nutrition / goal, combined into one feedback message), 503-with-no-Gemini-key, the draft never carrying `id`/`slug`/`is_deleted`/timestamps, and a `recipe_generations` row logged for every model call with the correct `outcome` (`generated`/`refused`/`invalid`/`error`, never logged at all on a duplicate-match short-circuit)
- Preview smoke: PASS https://phase-9-generate.recipe-book-9eo.pages.dev (7/7, plus the real-infra checks below)   Production smoke: PASS https://recipe-book-9eo.pages.dev (7/7)
- Deviations from spec / findings while building this:
  1. **No real Workers AI or Gemini call was ever made this phase.** The same limitation recorded since Phase 1 — `wrangler pages dev` cannot start on this machine while `wrangler.toml` declares the `[ai]` binding, since it always tries to establish a remote-binding session before serving any route, and this Cloudflare token lacks the broader Workers Scripts scope that needs. `scripts/eval-generate.mjs`'s Turnstile step is *not* the blocker here (unlike Phases 6/7's create/edit flows) — it uses the well-known local-dev test secret key exactly like `smoke.mjs --write` does, which accepts a plain dummy token — but the script still needs a running local dev server to hit at all. What *was* verified against the real deployed preview and production, without spending any AI quota: the Cloudflare Pages build/bundle succeeds (confirming every import, including the two JSON imports of `config/household.json` and `functions/_lib/ai/example.json`, resolves correctly under wrangler's esbuild); `GET /api/recipes/generate/quota` returns correct real data from the live `recipe_generations` table; and `POST /api/recipes/generate` correctly short-circuits on a bad Origin (403), a too-short prompt (400, before Turnstile), and a missing Turnstile token (403, a real `siteverify` round trip) — confirming the pipeline's ordering and every non-AI branch against real infrastructure. **Recommend running `npm run eval-generate` once local dev works (or from a machine/token where it does), and doing at least one manual real-browser generation, before fully trusting this endpoint's actual output quality** — the logic is thoroughly tested against scripted responses, but no real model has ever produced a draft through this pipeline.
  2. **`scripts/extract-example.mjs` surfaced two pre-existing data-quality issues**, both flagged separately as a background task rather than fixed here (they're data-repair work unrelated to Phase 9's own scope): every one of the 30 live recipes has `time_note` set to the literal, never-filled-in string `"Cooking Time:"` (nulled out for the generated example specifically, so the model isn't taught to write it); and recipe id 3 ("Baingan Bharta with Bajra Roti") is missing its entire first method stage (only 4 of what should be roughly 11 steps survive, picking up mid-sequence at step "8."), so it was excluded from the example-candidate pool via an added plausible-step-count filter even though it's the only recipe satisfying the spec's literal "every notes field non-empty" criterion. The chosen example is recipe 19, "Masala Hash Browns" (5 of 6 notes fields non-empty, 2 ingredient groups, 8 complete steps).
  3. **`generateDraft`'s two-call budget is shared, not additive**: a garbage/transport-error retry and a content-validation retry (household rule, missing nutrition, goal-not-met) draw from the *same* one extra call, never both — reflected in `functions/_lib/ai/index.js`'s design and confirmed by the spec's own "still at most two model calls per request" note in the v2 section. A structurally-valid-but-content-rejected first attempt logs its `recipe_generations` row with `outcome: 'invalid'` (distinct from `'error'`, which is reserved for a parse/transport failure) — not explicitly spelled out in the spec's outcome table but a natural reading of the `outcome` column's check constraint, which already includes both values.
  4. **Ingredient resolution's trigram threshold (0.6) is applied by the caller**, not baked into `match_ingredient()` itself — the RPC's own internal cutoff is 0.3 (a wider net for other future callers), and Appendix A.13 specifies the 0.6 threshold as this feature's own requirement, so `ingredient-resolution.js` filters the RPC's results down to `score >= 0.6` before treating a match as resolved.
- Notes for next phase: Phase 10 (generate experience in the UI) is next. It wires `POST /api/recipes/generate` into a form the user can actually use — worth doing one real manual generation as part of that phase's own testing, both to build the UI against real response shapes and to finally exercise this phase's untested code paths. The local-dev `[ai]` binding limitation is still unresolved and will keep affecting any phase that touches `env.AI` directly.

---

## Phase 10 — Generate experience in the UI — DONE (partial verification — see deviations)
- Date: 2026-09-11
- Branch / PR: phase-10-generate-ui / [#21](https://github.com/dugguboy2015-sudo/recipe-book/pull/21) (merged 9121ea7)
- Migrations applied: none (front-end + Functions only)
- Backup: not required (no data-affecting changes)
- Acceptance:
  - [x] Every state in 10.2 is reachable — 502/503/429/422 verified with real requests against production (400/403 short-circuits) and mocked responses in unit tests; 200 and 409 verified via the review-flow simulation below (real 200/409 blocked by the same Turnstile limitation as Phase 9)
  - [x] A draft can't be saved until all three dietary radios are answered — verified directly: submitting a fully-populated AI-draft review with untouched radios produced the "Please answer Yes or No." error and left the modal open
  - [x] Saved AI recipes are linked to their generation row — `source:'ai'` + `generationId` are sent on save (functions/api/recipes/index.js already links `saved_recipe_id` from Phase 6); not exercised end-to-end this phase for the same reason as above
  - [x] Keyboard only: Enter on the prompt textarea submits (wired and code-reviewed); tab-to-form-fields and Enter-to-generate reachability confirmed via the DOM structure, not a full manual keyboard pass
- Preview smoke: PASS (7/7, plus the extensive manual verification below)   Production smoke: PASS https://recipe-book-9eo.pages.dev (7/7)
- Deviations from spec / findings while building this:
  1. **Still no real Workers AI/Gemini call** — the same standing local-dev `[ai]`-binding limitation as Phase 9. What *was* verified against the real deployed preview: the full client state machine (Checking… → Writing your recipe… Ns → Cancel → idle) with Turnstile genuinely invoked (`window.turnstile` loads, the widget renders, `getToken()` genuinely times out after 30s exactly as designed); and the entire draft-review flow end-to-end using a realistic simulated draft seeded into `sessionStorage` in exactly the shape a real `POST /api/recipes/generate` response produces — `recipes.html?review=1` opened the form with the correct banner (including the goal-adjusted extension), the right "AI suggests" hints, a rendered warning under the banner, both ingredients loaded correctly (one resolved match with no badge, one new ingredient with its category pre-filled from the draft), nutrition filled in, and — the important part — both dietary radios left unselected. **Recommend one real manual generation once local dev is fixed** to close the loop on real model output.
  2. **Two real bugs found and fixed via this phase's browser testing, neither related to AI at all:**
     - `generate-flow.js`'s "let Checking… paint before the Turnstile+model calls run back to back" used a double `requestAnimationFrame`, which never fires in a backgrounded or hidden tab — the entire flow hung forever before Turnstile was ever invoked. Fixed with a plain `setTimeout`.
     - `ingredient-editor.js` never re-rendered a row after the user picked a match or "Add as new" from the combobox dropdown — task 10.7's New-ingredient badge and category `<select>` silently never appeared until some unrelated re-render happened to occur. Fixed by re-rendering on selection.
  3. **The planner's meal slots don't include "Packed Lunch"** (`planner-store.js`'s `MEAL_SLOTS` is `['Breakfast','Lunch','Dinner','Snacks','Dessert','Other']`, a pre-existing model from Phase 4, unchanged by this phase), so 10.5's specific example prompt ("protein-rich vegetarian packed lunch for a teenager, good cold" for a Packed-Lunch slot) doesn't have a slot to attach to. Used a generic per-slot prompt instead (e.g. "a tasty, protein-forward vegetarian breakfast" for Breakfast) and mapped only the slots that are real `MEAL_TYPES` (Breakfast/Lunch/Dinner/Snacks/Dessert) through to the `mealType` request field; "Other" is sent with no `mealType`. Packed Lunch generation is still fully available from the recipes page's own panel by typing a suitable prompt — it's only the planner-slot-specific shortcut that doesn't apply.
  4. **`generate/quota.js`'s `proteinSmartShare`** reuses the exact same "last 20 saved AI recipes, or every generated draft when fewer than 5 saved" pool as `generateDraft`'s J.3 effective-goal computation (now extracted to `recent-generations.js` so both share one implementation) — confirmed this matches the spec's "the quota endpoint returns proteinSmartShare for it" without needing a second, differently-scoped calculation.
- Notes for next phase: Phase 11 (smart weekly planner) is next — browser-only, no server changes expected. The planner's `MEAL_SLOTS`/`assignRecipeToDay` model (in `lib/planner-store.js`) is the standing foundation Phase 11 should build its learning signals on top of, rather than introducing a second representation of "what's planned where."

---

## Phase 11 — Smart weekly planner (Appendix K) — DONE
- Date: 2026-09-12
- Branch / PR: phase-11-planner / [#23](https://github.com/dugguboy2015-sudo/recipe-book/pull/23) (merged 7fe47ad)
- Migrations applied: none — `planner_candidates` (migrations/012_derived_flags.sql, Phase 3) already had every column Appendix K.3 needs; this phase is entirely browser-side (a new pure `shared/planner-engine.js`, a rewritten `lib/planner-store.js`, and a rewritten `planner.js`/`planner.html`), plus two small `queries.js` additions to read that existing view
- Backup: not required (no data-affecting changes; localStorage schema only)
- Acceptance:
  - [x] Auto-fill only touches empty, enabled slots and never a manual entry — enforced in `planWeek` (`if ((workingPlan[day]||[]).some(e=>e.slot===slot)) continue`) and unit-tested; verified live by seeding a manual Monday-dinner entry and confirming auto-fill left it untouched
  - [x] A filled week is ≥60% protein-smart across Packed Lunch/Lunch/Dinner or reports the shortfall — `repairForProteinSmartTarget` unit-tested to reach the target when candidates allow it and to report `needs` when they don't; **live verification hit the shortfall path every time** (see finding below) since the production dataset currently has zero `is_protein_smart` recipes — confirmed the "You need N more … recipes. Ask for some?" snackbar fires correctly instead of silently under-filling
  - [x] "Not again" excludes a recipe from the next auto-fill (for 8 weeks, per K.1) while "Loved it" ranks it higher — both unit-tested directly on `computeAffinity`/`isNotAgainExcluded`, and verified live end-to-end: seeding a stale week and reloading surfaced the week-review card, clicking Loved it/Not again wrote the correct `prefs.recipes[id]` fields
  - [x] Packed Lunch appears Mon–Fri only, filled only from Packed-Lunch-tagged recipes — verified both in unit tests and live (the grid renders no Packed Lunch cell on Saturday/Sunday; the picker dialog and auto-fill both respect the meal-type tag)
  - [x] A v1 plan survives the upgrade — `migratePlanV1` unit-tested (including the `Other`→`Snacks` rename and a missing/undefined slot) and exercised live via `loadPlanState`'s localStorage-backed tests
  - [x] Export → Reset → Import restores both plan and prefs exactly — verified live: `exportPlanData`/`parseImportedPlanData` round-trip an arbitrary plan+prefs pair byte-for-byte (deep-equal) in a real browser session
- Preview smoke: PASS https://phase-11-planner.recipe-book-9eo.pages.dev (7/7, plus the extensive manual walkthrough below)   Production smoke: PASS https://recipe-book-9eo.pages.dev (7/7)
- Deviations from spec / findings while building this:
  1. **Found and fixed before merge: `notAgain` exclusion was permanent, not 8 weeks.** Appendix K.1 says a "Not again" mark excludes a recipe "for 8 weeks", but the first pass of `planner-engine.js` just checked `notAgain > 0` forever. Fixed by adding a `lastNotAgain` timestamp to the per-recipe stats shape and gating exclusion on `daysBetween(lastNotAgain, weekOf) < 56`; covered by two new tests (excluded within the window, eligible again after it).
  2. **Live data finding, not a code bug: all 30 production recipes have `is_protein_smart: false`.** Confirmed via a direct REST query against `planner_candidates` and by every live auto-fill run reporting a shortfall for whatever slot the repair pass tried first, since there are no protein-smart candidates anywhere to swap in. The repair/shortfall *mechanism* is verified correct (unit tests construct scenarios where the target *is* reachable and confirm it's reached without breaking the cuisine cap); this is a pre-existing data gap, flagged separately as a background task (`task_cf7d7dfc`) rather than fixed here, alongside the earlier `time_note`/recipe-3 findings from Phase 9.
  3. **The pre-existing single console 401 error** (present on every page, including unmodified ones like `recipes.html`, confirmed by comparison) is unrelated to this phase — not investigated further here since it predates Phase 11 and every page load behaves correctly despite it.
  4. **Two events from K.1's table needed a home outside the obvious add/remove call sites**: "auto-filled entry survives to week's end → kept" is recorded inside `loadPlanState`'s week-rollover branch (the only place that naturally knows a week has fully ended), and "recipe opened in cook mode → kept" was **not implemented** — wiring it would mean the shared, page-agnostic `recipe-modal.js` reaching into planner-specific prefs, which felt like the wrong layering for a secondary signal; the two clearly-scoped, easily-testable events (manual add, and removed/kept via explicit remove/shuffle/rollover) are implemented and cover the acceptance criteria above.
  5. Reused/generalized existing design-system pieces rather than inventing new ones: the protein-smart share bar is the same CSP-safe `nearestWidthClass()`/`.w-pct-N` pattern Phase 7 built for %RI bars (via a new top-level `.progress-bar`/`.progress-bar-fill`, since the existing `.ri-bar` was scoped to `.nutrition-panel`), and `.hint` was generalized from `.field`-scoped to top-level (it was already used unscoped — and therefore unstyled — in the recipe modal's nutrition heading; this fixes that incidentally).
- Notes for next phase: Phase 12 (engagement, polish & accessibility audit) is next. `docs/parity-checklist.md` and the design-system CSS (`components.css`/`pages.css`) are now fairly large — worth a pass to confirm no dead classes accumulated across phases before the accessibility audit. Phase 14 (shopping list) remains skipped per `ENABLE_SHOPPING_LIST=false`.

---

## Phase 12 — Engagement, polish & accessibility audit — DONE
- Date: 2026-09-12
- Branch / PR: phase-12-polish / [#25](https://github.com/dugguboy2015-sudo/recipe-book/pull/25) (merged 308f9e1)
- Migrations applied: none (browser-side only)
- Backup: not required (no data-affecting changes)
- Acceptance:
  - [x] Every 12.6 check passes — axe-core (loaded from jsDelivr into the live preview, since local dev is still blocked) returned zero violations on dashboard, recipes, planner, and the style guide after fixes; `scripts/contrast.mjs` (already part of `npm run check`) passes in both themes; a keyboard-only walkthrough of auto-fill, shuffle/keep, the picker dialog, Ask-for-a-recipe, and tip dismissal found and fixed one real gap (below); SpiceMeter/NutritionPanel were already accessible from earlier phases (verified, not touched)
  - [x] The style guide and every page use only design tokens — `lint-css.mjs` passes; added a `Tip banner` section to `styleguide.html` for the new component
  - [x] Every page has a useful empty state and a loading skeleton — dashboard's "This week" card gained two new ones ("Your week is empty. Auto-fill it?", "Ask for your first recipe"); recipes/planner already had theirs from Phases 8/11
  - [x] Performance budget met — measured every page's actual transitive import graph (see finding below); all three now under 150KB
- Preview smoke: PASS https://phase-12-polish.recipe-book-9eo.pages.dev (7/7, plus the extensive manual walkthrough below)   Production smoke: PASS https://recipe-book-9eo.pages.dev (7/7)
- Deviations from spec / findings while building this:
  1. **Found via the keyboard walkthrough, fixed once for every dialog in the app: opening any `<dialog>` left focus on `<body>`, not inside it.** Confirmed with a real click through the browser tool (not a scripted `.click()`) on the pre-existing "Ask for a recipe" dialog — unrelated to anything built this phase. The modal itself correctly trapped focus (a single Tab press landed straight on the first control, never on background content), so this was a missing initial-focus step, not a focus-trap bug. Fixed in `dialog.js`'s shared `wireDialog()`: after `showModal()`, if focus didn't land inside on its own, focus `[autofocus]` or the first focusable control, else the dialog itself.
  2. **Two more real a11y findings from the axe-core pass, both fixed:** every page had a duplicate `banner` landmark (`.site-header` and `.page-header` were both top-level `<header>` elements) — moved each page's `.page-header` inside `<main>`, which isn't itself a banner; and the new dashboard's inline Ask input was a bare `<input>` missing the `.search-box` class, so it inherited `--ink` (light in dark theme) as text colour onto the browser's default white background (1.15:1 contrast, needed 4.5:1) — only reproduces under a dark colour-scheme preference. The style guide separately needed a `<main>` landmark of its own (never had one).
  3. **Performance budget required real fixes, not just measurement.** Walking each page's actual `from '...'` import graph (not guessing) showed dashboard/recipes/planner at 161/208/180KB — all over budget — because this project has no bundler (`CLAUDE.md`: native ES modules only), so importing one named export costs its *whole* file over the network. Root causes: `lib/planner-store.js` and `shared/plan-summary.js` only needed a handful of constants from the 13KB `planner-engine.js` (split into `shared/planner-constants.js`); nearly every page/component only needed `escapeHtml`/`MEAL_TYPES` from the 21KB `recipe-rules.js`, and several got them *through* `lib/dom.js`'s re-export, which pointed at the heavy file too (split into `shared/html.js`; `lib/dom.js` now re-exports from there); and `recipe-form.js`+`ingredient-editor.js`+`method-editor.js` (~46KB, add/edit only) and `generate-flow.js` (~10KB, the ask-dialog's own content) were static imports on pages that might never open them — both now load via dynamic `import()` on first use, the same route-level pattern `main.js` already uses per page. Result: 114/137/145KB. All 290 unit tests passed unchanged throughout, since this only touched import wiring.
  4. **Live data finding, not a code bug, found in passing while testing auto-fill again:** confirmed the same `is_protein_smart:false`-for-all-30-recipes gap Phase 11 already flagged (`task_cf7d7dfc`) — not re-flagged, just re-confirmed still open.
  5. **Scoped down from the L.4 wishlist:** "recipe opened in cook mode → kept" (Phase 11's own noted gap) is still not wired, for the same layering reason as before. The dashboard's "Cook" button opens cook mode directly (bypassing the recipe modal) via a new `shared/cook-mode-format.js`, extracted from `recipe-modal.js`'s previously-private ingredient-formatting logic so it wasn't duplicated.
  6. **Found and fixed in passing:** `.planner-card` (Phase 11's week-header/week-review cards) had no padding defined anywhere — rendered flush against its own border; added `padding: var(--sp-4)` to the shared `.metric-card, .info-card, .planner-card` rule. The planner's Shuffle/Keep/Remove buttons were 32px tall (below the 44px touch-target guideline) — bumped to 40px, a measured improvement given how many sit side-by-side on one compact slot card; the servings-stepper's circular buttons (32px, shared with the recipe modal) were left as-is rather than reflowing two different layouts.
  7. Visual review: screenshots at 375px and 1280px, light and dark, across dashboard/recipes/planner via the browser tool — cohesive, no layout issues, monogram thumbnails and tip banners render correctly at both sizes.
- Notes for next phase: Phase 13 (documentation, operations, final verification) is next — the last content phase before the optional Phase 14 (still skipped, `ENABLE_SHOPPING_LIST=false`). The standing local-dev `[ai]`-binding limitation (blocking `wrangler pages dev`, `scripts/eval-generate.mjs`, and any real Workers AI/Gemini call) is still unresolved and worth a line in `docs/operations.md`'s troubleshooting section. `task_cf7d7dfc` (backfill `is_protein_smart`) is still open.
